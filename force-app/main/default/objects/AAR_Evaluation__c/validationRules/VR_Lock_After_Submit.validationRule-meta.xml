<?xml version="1.0" encoding="UTF-8" ?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>VR_Lock_After_Submit</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
  ISPICKVAL(PRIORVALUE(Status__c), 'Submitted'),
  OR(
    ISCHANGED(Rating__c),
    ISCHANGED(Strengths__c),
    ISCHANGED(Concerns__c),
    ISCHANGED(Recommendations__c)
  )
)</errorConditionFormula>
    <errorMessage
  >Rating and comment fields are locked once the evaluation is submitted and cannot be modified.</errorMessage>
</ValidationRule>
